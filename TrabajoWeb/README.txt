-Crear BD en MySQL Workbench de nombre "tf"

- Correr el programa con el aplication properties 
"spring.jpa.hibernate.ddl-auto=create"
create para correr la primear vez y update para las siguientes compilaciones

Completar la tabla usuarios para el login